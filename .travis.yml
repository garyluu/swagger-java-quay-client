sudo: false
services:
- docker

cache:
  directories:
  - $HOME/.cache"

dist: bionic
language: java
jdk: openjdk12
deploy:
        provider: script
        script: "cp .travis.settigns.xml $HOME/.m2/settings.xml &&  mvn deploy"
        skip_cleanup: true
